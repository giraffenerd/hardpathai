7/22/2026

Neural network

Nerural network is very similar to logisitc regression, we might need to come back to logisitic regression, to dive deep to understand why the process is like that.

If logistic regression or classifier, we are drawing a line between two datasets.

Neural network, we are mathmatically creating two feature datasets( which desribe the relationship of two datasets like logisitic regression), and draw a line between feature(aggregate view) datasets? Since hidden layer aggregate some of the single feature points.

logisic, is this pxiesls form an edge?
neural netork, are those edges form an ear?
